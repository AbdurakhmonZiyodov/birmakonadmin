import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    modalItemText: {
        fontSize: 16,
        lineHeight: 20,
        fontWeight: "500",
        color: "#313131"
    },
    modalItem: {
        paddingVertical: 10,
        paddingHorizontal: 20,
    },
})